# 下一轮 Codex 启动提示

把下面整段复制到新的 Codex 对话中：

```text
你现在接手“南枫八字”Android App 的后续维护。

仓库：
/Users/nanzhufeng/Documents/工具开发/nanfeng-bazi

先只读执行：
1. 确认 Git 根目录、当前分支和工作区状态。
2. 完整读取 AGENTS.md 和 docs/CURRENT_HANDOFF.md。
3. 只读取本次门禁复核相关的 docs/REQUIREMENT_GAP_AUDIT.md、
   docs/architecture-governance.md、docs/domain-rules.md、docs/TEST_STRATEGY.md 和
   docs/decision-log.md 片段。
4. 使用 developing-apps-with-governance 和
   develop-apps-with-nanfeng-product-standards Skill。

现场代码和最新测试证据优先于交接文档；若不一致，先报告并修正文档。不要读取旧对话
全文，不要把其他 App 的事实带进本项目。

当前唯一任务：从 alpha68 的真实用户列表复核门禁继续，逐条确认日期/四柱冲突与缺失。

当前事实：
- v1.0 核心、VX-01～VX-10 已完成当前自动化与 API 35 模拟器证据。
- alpha68 两张授权列表稳定拆出 49 行，姓名/性别 49/49；43 条合法完整 OCR 四柱中
  30 条同日一致、13 条冲突，6 条未完整，其中 1 条来源本身以星号隐去时柱。
- 冲突与缺失必须按原图逐条确认；不得用历法猜值或把格式完整冒充日期正确。
- 其他剩余项为 IM-13/QA-07 其他真实页面样本、VX-11 外部 AI、QA-08 OPPO 和 QA-10
  正式签名/发布门禁。

执行规则：
1. 若用户提供并明确批准新的真实问真样本，只处理被批准的页面类型，先做脱敏和分层
   指标，不把样本准确率外推到其他页面。
2. 若用户明确批准 VX-11，先确认联网、外部服务、字段范围、脱敏预览、主动确认、来源
   标记和失败回滚，再实施；没有这些授权不得联网或接 AI。
3. 若用户明确批准 OPPO，只允许同签名覆盖安装并保留数据；不得卸载或清数据。
4. 若用户明确批准签名/发布，再读取对应材料和发布合同；默认不 push、不发布。
5. 若没有新的逐条确认，保持 alpha68 代码不变，更新现场状态后结束本轮复核；不要创造
   新需求或为了继续开发而升级 alpha。

安全边界：
- 不操作未获授权真机，不联网，不接外部 AI，不发送命例资料。
- 不提交真实姓名、八字、截图、密钥或构建产物。
- 不 push、不发布；保护任何现场未提交改动。
- 设备测试若获准仍须限定 emulator-5554，除非用户另行明确授权 OPPO。

复核最低证据：
- Git 根、分支、工作区条目数和最新本地提交。
- REQUIREMENT_GAP_AUDIT 的未完成项与授权边界。
- 当前 ADB 设备只读列表；不得因此自动启动设备写操作。
- 分开报告已确认事实、仍受门禁项和本轮未执行动作。
```
