# 下一轮 Codex 自动交接卡

下次由 Codex 直接读取本卡继续，不要求用户重复发送：

```text
你现在接手“南枫八字”Android App 的后续维护。

仓库：
/Users/nanzhufeng/Documents/工具开发/nanfeng-bazi

先只读执行：
1. 确认 Git 根目录、当前分支和工作区状态。
2. 完整读取 AGENTS.md 和 docs/CURRENT_HANDOFF.md。
3. 只读取本次门禁复核相关的 docs/REQUIREMENT_GAP_AUDIT.md、
   docs/architecture-governance.md、docs/domain-rules.md、docs/TEST_STRATEGY.md 和
   docs/decision-log.md 片段。
4. 使用 developing-apps-with-governance 和
   develop-apps-with-nanfeng-product-standards Skill。

现场代码和最新测试证据优先于交接文档；若不一致，先报告并修正文档。不要读取旧对话
全文，不要把其他 App 的事实带进本项目。

当前任务：保持 alpha70 本地基线，按实际新增输入解除外部门禁；不要再要求用户发送
“继续”。没有新输入时只做一次现场复核，不重复把禁止项报告成总目标受阻。

当前事实：
- v1.0 核心、VX-01～VX-11 已完成当前本地自动化与 API 35 模拟器证据。
- alpha70 的 VX-11 是无网络手动桥接：复用客观摘要字段投影，支持字段选择、默认脱敏、
  精确预览、主动确认复制、来源/模型/材料编号结果回填、过期拒绝和正式分析历史。
- App 不含网络权限、外部 AI 客户端或服务端依赖；自动联网/具体服务适配器仍需新授权。
- alpha69 两张授权列表稳定拆出 21+29 共 50 条完整日期候选，身份 50/50；39 条合法
  完整 OCR 四柱全部同日复算一致，冲突 0；11 条未完整，其中 1 条来源以星号隐去时柱。
- 其余门禁为 IM-13/QA-07 真实样本、QA-08 OPPO 和 QA-10 正式签名/发布。

执行规则：
1. 用户提供并批准新的真实问真样本时，只处理被批准页面类型，先脱敏和分层指标，不把
   准确率外推到其他页面；缺失值不得用历法猜造。
2. 用户批准 VX-11 自动服务适配器时，先确认网络、服务、字段和失败回滚，必须复用现有
   预览、默认脱敏、主动确认和来源标记合同；没有授权不得联网或发送资料。
3. 用户批准 OPPO 时，只允许同签名覆盖安装并保留数据；不得卸载或清数据。
4. 用户批准签名/发布后再读取材料和发布合同；默认不 push、不发布。
5. 没有新输入时保持 alpha70，不擅自创造需求、升级 alpha 或重复机械续跑。

安全边界：
- 不操作未获授权真机，不联网，不调用外部 AI，不发送命例资料。
- 不提交真实姓名、八字、截图、密钥或构建产物。
- 不 push、不发布；保护任何现场未提交改动。
- 设备测试若获准仍限定 emulator-5554，除非另行明确授权 OPPO。
```
