# 南枫八字 Android 增量开发经验正式审计

## 1. 审计结论

- 第一用户任务：用户能立即进入正确的命例详情、完整保留断事笔记，并在不牺牲本机数据的前提下获得可靠的加密备份。
- 当前完成定义：当前 checkpoint 已通过自动门禁、同签名覆盖安装与本机数据保留核验；不将未授权的真实云端写入宣称为已完成。
- 本次沉淀的核心判断：高频缓存与同步优化必须先绑定数据所有权与真实资源上限；“页面能显示”或“网络可连通”均不能替代内容一致性、快照可恢复性和真实服务验收。

## 2. 最终基准与明确排除项

| 证据层 | 最终基准 |
|---|---|
| 源码提交 | `b16a14c`（2026-08-12 checkpoint） |
| 正式构建 / 哈希 | Debug APK `f1282c80af15da73d0287ae1c4e567d8ae404537698706b39d1f6c15a697852a` |
| 自动门禁 | `test + assembleDebug + lintDebug`，253 项 Gradle 任务通过 |
| 模拟器 / 集成 | 本次增量采用 JVM 体量回归；不把它替代为完整 UI 或云服务验收 |
| 真实设备 / 服务 | OPPO 同签名覆盖成功；未启动、未执行真实云端上传、覆盖或恢复 |
| 数据保留 | 安装前后 `nanfeng-bazi.db`、WAL、SHM inode 与大小一致 |
| 当前工作树未验证增量 | 仅 `tmp/imagegen/` 生成任务记录，未纳入基线 |

明确排除：

- 已完成并已有专门决策/交接记录的皮肤、导出、菜单和排盘视觉迭代不在本次重复沉淀范围。
- 早期“网络失败”界面、被否定的空笔记表现及旧版双端冲突流程不能作为最终产品基准。
- 未授权的真实云端写入、跨设备恢复和 Google 服务端配置不作为本 checkpoint 的服务验收。

## 3. 分类口径

- A 项目专属事实：南枫八字的数据范围、`caseId + revision` 详情聚合、`BaziCloudSnapshotBridge` 与备份文件格式。
- B 跨项目可复用规则：缓存子状态所有权、流式快照、取消语义与验证证据分层。
- C 用户明确长期偏好：未授权时不触发真实云端数据操作；已单独提交长期记忆增量说明。
- D 平台 / 设备约束：Android WorkManager、Room、OPPO 同签名覆盖与数据保留边界。
- E 尚未验证推测：当前真实云端账户、网络与跨设备恢复环境下的吞吐和恢复耗时。

## 4. 反馈—原因—实现—验证证据矩阵

| 维度 | 用户反馈 / 原问题 | 首个语义分叉 / 根因 | 最终实现 | 自动测试 | 模拟器 / 集成 | 真实设备 / 服务 | 尚存风险 | 分类 |
|---|---|---|---|---|---|---|---|---|
| 功能与状态 | 切换案例后笔记看似全部消失 | 缓存只更新 `detail`，笔记草稿未按案例所有权更新 | 聚合快照原子装载，草稿绑定 `caseId + revision`，错配禁止渲染和保存 | A/B 同 revision、预取、进程恢复、1001 案例与空缓存回归 | JVM 通过 | OPPO 只读核对真实师傅点评与数据库一致 | 未覆盖每一种 OEM 进程回收路径 | A、B、D |
| 架构与数据 | 空草稿可能覆盖已有笔记 | 将“内存空”错误解释为“数据库无数据” | 数据库非空时阻止空草稿保存并重新 hydrate；异步回写再次校验所有权 | `StageTwoViewModelTest` 状态护栏 | JVM 通过 | 无写入验收，避免修改真实命例 | 后续新子表须接入完整案例聚合 | A、B |
| 性能 | 大库同步频繁显示失败 | 全表 `Json.encodeToString` 在主线程申请约 48 MiB 连续内存，进程退出后被误报为网络问题 | 逐条流式编码、两次顺序写出清单与 ZIP、快照切到 IO | 超过 16 MiB 笔记快照的生成和空库恢复回归 | JVM 通过 | OPPO 日志确认历史根因；本轮未启动同步 | 真实云端传输耗时尚未测量 | A、B、D、E |
| 错误与可观测性 | `JobCancellationException` 与网络失败混淆 | 取消被一般异常路径吞掉 | 取消原样传播并恢复持久状态；页面只呈现中文可行动状态 | 同步策略测试 | JVM 通过 | 无真实任务取消复测 | OEM 终止策略仍需现场验收 | B、D、E |
| 账号与外部集成 | 不能为了排查同步改写云端数据 | 安装/调试与真实上传权限混淆 | 本机为真、云端仅加密快照；真实上传、覆盖和恢复分开授权 | 加密、篡改拒绝、附件排除与策略测试 | JVM 通过 | OPPO 覆盖安装且数据保留；未调用服务 | Google/Supabase 全链路待单独授权 | A、B、C、D |
| 交付 | 已完成工作需要可回退状态 | 大量未提交改动缺乏冻结基线 | 建立 `b16a14c` checkpoint，临时生成记录排除在外 | `git diff --check` 与全量 Gradle 门禁 | 不适用 | 同签名安装证据与代码提交分开记录 | 尚未推送远端或建立 Release | A |

## 5. 跨项目规则

1. 触发条件：详情页缓存包含一个父对象和多个可编辑子对象。执行规则：所有可渲染/可保存子状态必须绑定同一对象身份和版本；任一错配时禁止渲染旧草稿并重新 hydrate。验证方法：同版本对象快速切换、预取命中、进程恢复、背景子表更新与空缓存/非空数据库。禁止项：只比较父 revision，或把内存空值作为持久化为空的证据。
2. 触发条件：移动端自动备份需要序列化大量本地记录。执行规则：以稳定顺序流式计数、哈希和写出，不创建整表 JSON 字符串；序列化在 IO 调度器。验证方法：大于常规单项阈值的数据生成、解压、哈希及恢复回归。禁止项：把整库 `encodeToString`、主线程编码或用“网络失败”掩盖资源耗尽。
3. 触发条件：协程或后台任务被页面离开、应用更新或系统调度停止。执行规则：取消保留为控制流，恢复真实已知状态；只把可重试的真实外部错误归为网络失败。验证方法：取消、重试和后续业务变更的调度测试。禁止项：捕获所有异常后统一显示连接失败。

对第三方服务，代码已接入、可配置和已在真实账户完成服务动作是三个独立等级；缺少最后一级证据时不得宣称云服务可用。

## 6. 项目专属事实与遗留风险

### 已确认项目专属事实

- 详情与笔记一致性规则的唯一实现入口与回归清单见 [architecture-governance.md](architecture-governance.md) 和 [decision-log.md](decision-log.md)。
- 云端结构化备份的安全范围、恢复条件与状态文案见 [cloud-sync-contract.md](cloud-sync-contract.md)。

### 平台 / 设备约束

- OPPO 仅支持同签名主 APK 覆盖验收；自动化测试限于隔离模拟器，不能在 OPPO 安装测试包。

### 尚未验证的遗留风险

- 实际 Google/Supabase 上传、回读哈希、跨设备恢复与长时间网络抖动待用户单独授权后验收。

## 7. 失败经验与防回归资产

| 被否定做法 | 失败原因 | 今后禁止项 | 机器防回归 |
|---|---|---|---|
| 仅刷新父详情缓存 | 子状态遗留，制造数据假空白 | 不得让父对象缓存单独决定子对象可见性 | `StageTwoViewModelTest` 的案例所有权场景 |
| 全表 JSON 一次性编码 | 连续内存峰值导致进程退出 | 不得对自动同步的全库表调用整表字符串序列化 | `CaseBackupServiceTest` 的大笔记快照生成/恢复 |
| 取消统一归类为网络失败 | 用户无法判断真实状态且误操作重试 | 不得吞掉 `CancellationException` | 云同步策略测试 |

## 8. 产物清单

- context：[project-context.md](project-context.md)
- 架构所有权：[architecture-governance.md](architecture-governance.md)
- 交接 / 决策：[CURRENT_HANDOFF.md](CURRENT_HANDOFF.md)、[decision-log.md](decision-log.md)
- 共享 Skill / SOP：`nanfeng-google-account-sync` 的大库快照与取消语义规则。
- 模板：复用个人 App 经验审计模板，不复制项目业务内容。
- 自动检查：`validate_app_experience_distillation.py`、全量 Gradle 门禁与现有状态/备份回归。
- 长期记忆更新：仅记录跨项目缓存所有权与流式快照规则，不写项目路径、真实数据或账户信息。

## 9. 验证等级

| 等级 | 命令 / 环境 | 结果 | 不能替代的更高等级 |
|---|---|---|---|
| 文档与 Skill | 审计结构、链接、秘密模式和工作流审计 | 待本次检查执行 | 自动测试 |
| 自动测试 | `./gradlew test assembleDebug lintDebug` | 253 项任务通过 | 真机与真实云服务 |
| 构建 | Debug APK 与 SHA-256 | 已生成并记录 | 同签名设备安装 |
| 模拟器 / 集成 | JVM 大快照与状态回归 | 已通过 | OEM 真机与外部服务 |
| 真实设备 / 服务 | OPPO 覆盖安装与数据 inode | 安装/保留通过；真实云服务未执行 | 已授权上传、回读、恢复 |
