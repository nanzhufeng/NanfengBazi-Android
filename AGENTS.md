# 南枫八字 Android 长期开发契约

## 事实与范围

- 当前用户指令优先；动手前读取与改动相关的 `docs/PRODUCT_REQUIREMENTS.md`、`docs/architecture-governance.md`、`docs/domain-rules.md`、`docs/TEST_STRATEGY.md` 和 `docs/CURRENT_HANDOFF.md`。
- 代码、自动化验证和已验收的设备证据优先于文档叙述；发现冲突要记录，不能悄悄沿用过期文档。
- `CURRENT_HANDOFF.md` 只承载临时状态，`decision-log.md` 承载可追溯决策；本文件只保留长期规则。

## 架构与领域边界

- 排盘唯一生产入口是 `BaziEngine.calculate()`；页面不可临时推演或复制算法。引擎、真太阳时与专业岁运变更必须保留并扩展黄金样本和边界测试。
- 命例写入只经 `CaseRepository`；Room 是本机业务真值。不得绕过仓储直接改 Room，也不得 destructive migration。
- 详情、草稿、记录和事件必须绑定同一 `caseId + revision`；内存空值不能当作持久化数据为空。
- 完整备份、恢复和云快照只经 `CaseBackupService`。导入、预览和恢复必须先校验路径、哈希、数量、引用和冲突计划；不得把预览伪装成写入。
- 合盘、格局、旺衰等结果必须由领域层的可测试事实生成；UI 仅呈现结果。面向普通用户的文案不得伪造确定性结论或把固定模板冒充个案判断。

## 数据、隐私与外部能力

- 不提交真实命例、附件、OCR 原文、账号、恢复码、令牌、密钥、云密文或诊断中的可识别数据。
- AI 与云端调用必须由用户主动触发、明确展示发送范围。密钥仅可经 Android Keystore 保存；外部结果是可追溯记录，不得覆盖本机计算真值。
- 云同步只传加密结构化快照，不传来源图片、附件、字段证据或模型密钥；真实上传、回读和恢复需要单独授权与单独报告。
- 取消是正常控制流，不能显示为网络错误或内部异常。

## UI 与交互

- 不为视觉重排复制 ViewModel、用例、排盘、导入、备份或同步逻辑；修改应保持现有导航、保存与返回链路。
- 有形状的可交互控件必须以同一形状裁切表面、点击、涟漪和按压反馈；新增入口要补相应 UI/交互合同测试。
- 折叠屏内外屏必须使用明确分支或参数，不能以共享偏移覆盖两种布局；可滚动内容必须为悬浮主导航预留实际可达的尾部空间。

## 验证、设备与交付

- 先做最小充分的 JVM/静态/构建验证，再按需要做隔离模拟器或真机验证；报告必须区分代码、测试、构建、设备和真实外部服务证据。
- 永久禁止执行任何 `connected*AndroidTest`。OPPO 等主设备不得安装测试包、卸载、清数据、直接操作数据库或自动打开应用。
- 覆盖安装只在用户明确授权后进行：先核对设备、包名、版本、`DEBUGGABLE`、签名和数据指纹；仅同签名主 APK 使用 `pm install -r --user 0`。版本递增不等于签名兼容。
- 提交前运行与改动相称的测试、构建和 `git diff --check`；构建成功不等于真机、真实问真或云服务验收。
